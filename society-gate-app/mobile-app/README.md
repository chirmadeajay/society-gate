# Society Gate - Mobile App (Expo)

## Setup
1. Install Expo CLI: `npm install -g expo-cli`
2. `npm install`
3. Edit `App.js` → change `API_URL` to your backend IP
4. Run: `expo start`
5. Scan QR with Expo Go app on Android/iOS
